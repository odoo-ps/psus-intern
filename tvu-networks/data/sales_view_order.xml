<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="cron_cleanup_expired_quotations_tvu_networks" model="ir.cron">
            <field name="name">Auto Cancel The Expired Quotations</field>
            <field name="model_id" ref="model_sale_order"/>
            <field name="state">code</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=0,minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="code">model._auto_cancel_expired_record()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
        </record>
     </data>
</odoo>