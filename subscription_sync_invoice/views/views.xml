<odoo>
  <data>
    <!-- <template id="sale_subscription_view_form"
      name="subscription_sync_contact_field"
      inherit_id="sale_subscription.sale_subscription_view_form">
      <xpath expr="//group[@name='main']/group[2]" position="inside">
        <field name="contact_id" />
      </xpath>
    </template> -->
    <record id="sale_subscription_view_form_inherit_subscription_sync_invoice" model="ir.ui.view">
      <field name="name">sale.subscription.view.form.inherit</field>
      <field name="model">sale.subscription</field>
      <field name="inherit_id" ref="sale_subscription.sale_subscription_view_form" />
      <field name="arch" type="xml">
        <field name="template_id" position="after">
          <field name="contact_id" />
        </field>
      </field>
    </record>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="subscription_sync_invoice.list">
      <field name="name">subscription_sync_invoice list</field>
      <field name="model">subscription_sync_invoice.subscription_sync_invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="subscription_sync_invoice.action_window">
      <field name="name">subscription_sync_invoice window</field>
      <field name="res_model">subscription_sync_invoice.subscription_sync_invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="subscription_sync_invoice.action_server">
      <field name="name">subscription_sync_invoice server</field>
      <field name="model_id" ref="model_subscription_sync_invoice_subscription_sync_invoice"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
    <!--
    <menuitem name="subscription_sync_invoice" id="subscription_sync_invoice.menu_root"/>
-->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="subscription_sync_invoice.menu_1" parent="subscription_sync_invoice.menu_root"/>
    <menuitem name="Menu 2" id="subscription_sync_invoice.menu_2" parent="subscription_sync_invoice.menu_root"/>
-->
    <!-- actions -->
    <!--
    <menuitem name="List" id="subscription_sync_invoice.menu_1_list" parent="subscription_sync_invoice.menu_1"
              action="subscription_sync_invoice.action_window"/>
    <menuitem name="Server to list" id="subscription_sync_invoice" parent="subscription_sync_invoice.menu_2"
              action="subscription_sync_invoice.action_server"/>
-->
  </data>
</odoo>