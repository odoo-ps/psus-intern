<?xml version="1.0" encoding="UTF-8"?>


<odoo>
    <data noupdate="1">
        <record id="quotation_expiry_checker" model="ir.cron">
            <field name="name">Quotation: Expiry checker </field>
            <field name="active" eval="True" />
            <field name="model_id" ref="model_sale_order"/>
            <field name="state">code</field>
            <field name="code">model.check_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 12:00:00')" />
            <field name="numbercall">-1</field>
        </record>
    </data>
</odoo>